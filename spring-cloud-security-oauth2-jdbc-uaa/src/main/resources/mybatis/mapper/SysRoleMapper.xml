<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xywei.mapper.SysRoleMapper">

    <resultMap id="sysRole" type="com.xywei.domain.SysRole">
        <id property="id" column="id"></id>
        <result property="roleName" column="role_name"></result>
        <result property="roleDescription" column="role_description"></result>
        <collection property="users" ofType="com.xywei.domain.SysUser">

        </collection>
    </resultMap>

    <select id="findRolesByUserName" parameterType="string" resultType="com.xywei.domain.SysRole">
    select * from sys_role
    where id in
    (
        select role_id from sys_user_role
            where user_id in
                (
                  select id from sys_user where username=#{username}
                )
    )
    </select>

</mapper>
